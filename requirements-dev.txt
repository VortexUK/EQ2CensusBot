# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Dev tooling (pre-push checks)
ruff>=0.5.0
pyright>=1.1.0
